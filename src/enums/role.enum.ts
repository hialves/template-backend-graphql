export enum Role {
  SuperAdmin = 'super_admin',
  Admin = 'admin',
  Client = 'client',
  Manager = 'manager',
  Employee = 'employee',
  NoRole = 'no_role',
  EmployeeNoLink = 'employee_no_link',
}
